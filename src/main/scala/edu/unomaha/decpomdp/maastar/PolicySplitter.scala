package edu.unomaha.decpomdp.maastar


class PolicySplitter {
  def splitOnDepth(children: Set[Policy], maxLayers: Int) : (Set[Policy], Set[Policy]) = {
    return (null, null)
  }
  
  def filter(policies : Set[Policy], belief : Belief) : PolicyFilter = {
     return null
  }
}